<% conference_meetings_by_months.each_with_index do |(time, meetings), index| %>
  var id = '<%= '#conference-day-tab-' + index.to_s %>';
  var html = '<%= j(render partial: "program_item", locals: { start_time: time[:start_time], end_time: time[:end_time], meetings: meetings.pluck(:meeting) }).strip.html_safe %>';
  console.log(id)
  console.log($(id))

  $(id).html(html);
<% end %>
