<tr>
  <td>
    <table class="header">
      <tbody>
        <tr>
          <td>
            <% if @custom_url_for_mail_root.present? %>
              <% url = @custom_url_for_mail_root %>
            <% else %>
              <% url = decidim.root_url(host: organization.host) %>
            <% end %>

            <%= link_to url do %>
              <%= image_tag "banner-email.png", alt: organization.name %>
            <% end %>
          </td>
        </tr>
      </tbody>
    </table>
  </td>
</tr>
